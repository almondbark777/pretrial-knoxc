# --- Secrets & credentials (NEVER commit) ---
.env
.env.*
!.env.example
*.pem
*.key
*.publishsettings
*.PublishSettings
.azure-publish-profile.xml

# --- Python ---
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
env/

# --- Local DB copies / working scratch ---
*.db
*.db-journal
*.db-wal
*.db-shm
*.sqlite
*.sqlite3
.claude/

# --- Source data with defendant PII (NEVER commit) ---
db/csv_clean/
# Reconcile/upload artifacts written next to the DB by reconcile_import.py and
# the /console/import upload page (per-row logs + staged CSVs carry names/IDNs).
db/import_logs/
db/import_uploads/
# EM-fee parity golden (derived from the offline DB; carries IDNs). Regenerate
# locally per PROJECT_HISTORY.md (Phase 9); the parity test skips when it's absent.
internal/db/testdata/emfees_golden.json

# --- EM-fee skill working scratch (regenerable; never commit) ---
.skill-unpacked/
.parity-venv/
.parity-tmp/
*.skill

# --- Build / deploy artifacts ---
*.zip

# --- Go build artifacts ---
/server
/ptr-server
*.exe
srv.log
srv.err

# --- OS / editor ---
.DS_Store
Thumbs.db
*.swp
.vscode/
.idea/

# --- Unpacked spec assets (regenerable via scripts/unpack.py) + tilde artifacts ---
/assets/
*.exe~

# deploy bundle (build artifact)
ptr1-deploy.tar.gz
*.tar.gz

# deploy bundle build output
deploy/dist/
/server-linux

# PTR tracker bundle edit scratch (unpack/repack workspace)
_tracker_work/

# scratch commit-message file used by the local workflow
COMMIT_MSG.txt
