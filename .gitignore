# --- Secrets & credentials (NEVER commit) ---
.env
.env.*
!.env.example
*.pem
*.key
*.publishsettings
*.PublishSettings
.azure-publish-profile.xml

# --- Python ---
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
env/

# --- Local DB copies / working scratch ---
*.db
*.db-journal
*.sqlite
*.sqlite3
.claude/

# --- Source data with defendant PII (NEVER commit) ---
db/csv_clean/

# --- Build / deploy artifacts ---
*.zip

# --- Go build artifacts ---
/server
/ptr-server
*.exe
srv.log
srv.err

# --- OS / editor ---
.DS_Store
Thumbs.db
*.swp
.vscode/
.idea/

# --- Unpacked spec assets (regenerable via scripts/unpack.py) + tilde artifacts ---
/assets/
*.exe~

# deploy bundle (build artifact)
ptr1-deploy.tar.gz
*.tar.gz
