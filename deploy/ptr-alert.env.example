# Copy to /etc/ptr-alert.env (root:root 0644 is fine — the topic is an alert
# channel, not a credential). Used by ptr-alert@.service and the backup
# freshness check to push ntfy notifications.
NTFY_URL=https://ntfy.sh/CHANGE-ME
